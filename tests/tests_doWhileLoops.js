describe('hardmode doWhileLoops.js', function() {
  it('is the most useless construct in JS', function() {
    expect(valueOfCountAfterLoop).to.be.equal(1);
    expect(valueOfCountAfterLoop).to.be.equal(count);
  });
});

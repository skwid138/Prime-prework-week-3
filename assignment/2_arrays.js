// write an array named 'people' that holds the following names: 'Albert', 'Dev', 'Ryan', 'Antoinette'
var people = [ 'Albert', 'Dev', 'Ryan', 'Antoinette' ];

// using array indicies, set the variable below equal to the first name in the people array
// e.g. var secondPerson = people[1]
var firstPerson = people[0];

// using an array property, set the variable below equal to the number of people in the array
var howManyPeople = people.length ;

// using array indices AND an array property, set the variable below equal to the last name in the array
var lastPerson = people[ people.length -1 ];

// add 'Prince' to the array
people.push( 'Prince' );
